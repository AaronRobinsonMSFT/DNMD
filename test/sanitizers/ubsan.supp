# Suppressions for UndefinedBehaviorSanitizer.
# CoreCLR's COM objects use C-style vtables that UBSan's vptr check
# does not recognize as valid C++ objects. Suppress only the vptr
# check and only for the files that call into CoreCLR COM interfaces.
vptr:*/test/regtest/main.cpp
vptr:*/test/regpal/pal.cpp
vptr:*/external/dncp/src/inc/dncp.h
